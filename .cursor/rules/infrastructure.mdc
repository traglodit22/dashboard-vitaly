---
description: VPS, деплой, синхронизация локально ↔ облако
alwaysApply: true
---

# Dashboard Vitaly — инфраструктура

Перед деплоем/SSH/секретами читай **`.local/SYNC.md`** и **`.local/ACCESS.md`** (gitignored).

## Синхронизация

- **GitHub**: `traglodit22/dashboard-vitaly`, ветка `main`
- **Локально**: `npm run dev`, `.env.local`
- **Прод**: http://135.106.161.215, `/var/www/dashboard`, `.env` на VPS
- **Деплой**: `git push` → GitHub Actions → `deploy/deploy.sh`
- **Не в git**: `.env*`, `.local/`, `bot/bot.js` (кастом на VPS)

## Прод-специфика

- Telegram через privoxy `127.0.0.1:8118`
- PM2: `dashboard` (auto-deploy), `dashboard-bot` (отдельно, `bot/bot.js`)
- Cron: `/usr/local/bin/dashboard-cron.sh` каждые 6 ч

## Локальная разработка

- `npm run db:setup` — локальный Postgres
- `npm run db:tunnel` — туннель к прод-БД (осторожно)
