/** Единые отступы страниц дашборда: компактно на телефоне, как в разделе «Файлы». */
export const DASHBOARD_PAGE_CLASS =
  "mx-auto w-full max-w-6xl p-3 pb-[max(0.75rem,env(safe-area-inset-bottom))] sm:p-6 lg:p-8";

/** Настройки: без max-width, контент на всю ширину main. */
export const SETTINGS_PAGE_CLASS =
  "flex min-h-full w-full flex-col p-4 pb-[max(1rem,env(safe-area-inset-bottom))] sm:p-6 lg:p-8";

export const DASHBOARD_PAGE_TITLE_CLASS = "text-xl font-semibold tracking-tight sm:text-2xl";
